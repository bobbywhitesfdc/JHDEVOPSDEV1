//
// Generated by JSON2Apex http://json2apex.herokuapp.com/
//
//  This Proxy Class is for a Single Line of GitMetdata
//          String json = '{"t":"CustomObject","n":"NBDS_eApp__c","cmm":"updated"}'
//       


public with sharing class CopadoGitMetadata implements Comparable {

	public String t;  // Type
	public String n;  // Name
	public String cmm;  // Change:  Create|Update|Delete

	public Integer compareTo(Object compareTo) {
        CopadoGitMetadata compareToObj = (CopadoGitMetadata) compareTo;
        if (t < compareToObj.t) {
        	return -1;
        } else if (t > compareToObj.t ) {
            return 1;
        } else if (n < compareToObj.n) {
        	return -1;
        } else if (n > compareToObj.n) {
        	return 1;
        } else {
        	return 0;
        }

	}
	
	public static CopadoGitMetadata parse(String json) {
		return (CopadoGitMetadata) System.JSON.deserialize(json, CopadoGitMetadata.class);
	}
	public static List<CopadoGitMetadata> parseArray(String json) {
		return (List<CopadoGitMetadata>) System.JSON.deserialize(json, List<CopadoGitMetadata>.class);
	}

}